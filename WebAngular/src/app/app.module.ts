import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { AppRoutingModule } from './app-routing.module'; // Додайте AppRoutingModule

@NgModule({
  declarations: [],
  imports: [
    BrowserModule,
    AppRoutingModule // Додайте AppRoutingModule
  ],
  providers: [],
})
export class AppModule { }